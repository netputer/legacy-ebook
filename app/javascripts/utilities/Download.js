/*global define*/
(function (window) {
    define([], function () {

        var downloadVideo = function (list) {
        
        };

        return {
            downloadVideo : downloadVideo
        };
    });
}(this));
