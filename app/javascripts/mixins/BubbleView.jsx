/** @jsx React.DOM */
/*global define*/
(function (window) {
    define([
        'React',
        '_',
        '$',
        'Wording',
        'GA'
    ], function (
        React,
        _,
        $,
        Wording,
        GA
    ) {
        var BubbleView = {

            getInitialState : function () {
                return {
                }
            },
            getBubbleClassName : function (name) {
            },
            closeBubble : function (type) {

            }
        }

        return BubbleView;
    });
}(this));
